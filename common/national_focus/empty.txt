#Added the way it is so there can be less errors in the log , this one so there can be less bloat or like ai actions effectivly making the mod a tiny bit faster and in size less

focus_tree = {
	id = empty_focus
	
	country = {
		factor = 0
		
		# NEUTRALS
		modifier = {
			add = 100
			tag = WST
		}
		modifier = {
			add = 100
			tag = SWE
		}
		modifier = {
			add = 100
			tag = HOL
		}
		# ANNEXED SOV FOOD
		modifier = {
			add = 100
			tag = EST
		}
		modifier = {
			add = 100
			tag = LAT
		}
		modifier = {
			add = 100
			tag = LIT
		}
		# ANNEXED NEUTRALS (AXIS FOOD)
		modifier = {
			add = 100
			tag = DEN
		}
		#BALKAN
		modifier = {
			add = 10
			tag = YUG
		}
		modifier = {
			add = 10
			tag = GRE
		}
		modifier = {
			add = 100
			tag = POL
		}
		modifier = {
			add = 1000
			tag = BEL
		}
		modifier = {
			add = 100
			tag = NOR
		}
		modifier = {
			add = 100
			tag = AUS
		}
		modifier = {
			add = 100
			tag = POR
		}
		modifier = {
			add = 100
			tag = TUR
		}
		modifier = {
			add = 100
			tag = ICE
		}
	}
	default = no
}